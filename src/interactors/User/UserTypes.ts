export interface UserData {
  id: string;
  name: string;
  icon_url: string;
}

export interface UserPublicData {
  id: string;
  name: string;
  icon_url: string;
}

export interface UserCreateData {
  id: string;
  name: string;
  icon_url: string;
}
